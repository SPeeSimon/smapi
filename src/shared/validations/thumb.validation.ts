const ImageSize = require('image-size');

class ThumbValidator {
    private thumbPath;
    
    // public validate() {
    //     var errors = [];
        
    //     {
    //         const dimensions = ImageSize(this.thumbPath);

    //         // Check if JPEG file is a valid JPEG file (compare the type file)
    //         if ($mime != "image/jpeg") {
    //             errors.push(new Error("Your thumbnail file does not seem to be a JPEG file. Please upload a valid JPEG file."));
    //         }

    //         // Check if thumbnail dimension is 320x240
    //         if (dimensions.width != 320 || dimensions.height != 240) {
    //             errors.push(new Error(`The dimension in pixels of your thumbnail file (${dimensions.width}x${dimensions.height}) does not seem to be 320x240.`));
    //         }
    //     }
    //     }
        
    //     return $errors;
    // }
}